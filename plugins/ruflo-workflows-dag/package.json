{
  "name": "@claude-flow/plugin-workflows-dag",
  "version": "0.1.0",
  "description": "Declarative DAG workflow engine — describe deterministic and AI-driven workflows in a single YAML file",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "schemas",
    "examples",
    "agents",
    "commands",
    "skills",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:test": "tsc -p tsconfig.test.json",
    "test": "npm run build:test && node --test build/tests/*.test.js"
  },
  "keywords": [
    "ruflo",
    "claude-flow",
    "workflow",
    "dag",
    "yaml"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.0"
  }
}
